KaronLabs ytdlp-korean-interface v2.19.1-karon.2
License and corresponding-source metadata

This directory is a technical compliance bundle, not legal advice.

Status: NOT_VERIFIED. The generators intentionally fail closed until all
blockers in the versioned lock are replaced by mechanically verified facts.
In particular, the final sealed candidate has not been supplied, the custom
7z.dll 26.01 binary has not been supplied, the Deno Rust/V8/native notice
corpus is not closed, and the BtbN FFmpeg external-library source cache is not
available with source-byte hashes.

License texts copied here came from the pinned source or binary archives named
in the versioned lock. Their SHA-256 values are recorded in that lock.
